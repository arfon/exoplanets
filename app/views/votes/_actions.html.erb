(Suggested by: <%= planet.user.name %> &middot; <%= pluralize(planet.votes.count, t(:votes)) %>)
<%- if current_user -%>
<%= button_to t(:vote), { :controller => 'votes', :action => "create", :planet_name_id => planet.id}, :remote => true %>
<%- else -%>
  <%= t(:require_login) %>. <%= link_to t(:login), new_session_path %>
<%- end -%>